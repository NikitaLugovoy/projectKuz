<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Меню</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .center-menu {
            text-align: center;
            max-width: 900px;
        }
        .menu-link {
            display: inline-block;
            margin: 10px;
            padding: 15px 30px;
            font-size: 18px;
            border-radius: 50px;
            background: white;
            color: #2575fc;
            text-decoration: none;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
            width: 100%;
            text-align: center;
        }
        .menu-link:hover {
            background-color: #2575fc;
            color: white;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
            transform: translateY(-3px);
        }
    </style>
</head>
<body>
    <!-- Центрированное меню -->
    <div class="center-menu">
        <div class="row">
            <!-- Первый ряд кнопок -->
            <div class="col-md-4 mb-3">
                <a href="/account/{{teacherId}}" class="menu-link">Аккаунт</a>
            </div>
            <div class="col-md-4 mb-3">
                <a href="/achievements/{{teacherId}}" class="menu-link">Достижения</a>
            </div>
            
            <div class="col-md-4 mb-3">
                <a href="/categories/{{teacherId}}" class="menu-link">Категория</a>
            </div>
            <div class="col-md-4 mb-3">
                <a href="/students/{{teacherId}}" class="menu-link">Студенты</a>
            </div>
            <div class="col-md-4 mb-3">
                <a href="/teachers/{{teacherId}}" class="menu-link">Преподаватели</a>
            </div>
            <div class="col-md-4 mb-3">
                <a href="/groups/{{teacherId}}" class="menu-link">Группа</a>
            </div>
        </div>
        <div class="row">
            <!-- Второй ряд кнопок -->
            <div class="col-md-4 mb-3">
                <a href="/parts/{{teacherId}}" class="menu-link">Мероприятие</a>
            </div>
            <div class="col-md-4 mb-3">
                <a href="/publicactivities/{{teacherId}}" class="menu-link">Общественная деятельность</a>
            </div>
            <div class="col-md-4 mb-3">
                <a href="/report-part-info/{{teacherId}}" class="menu-link">Информация о деталях отчета</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS и зависимости -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
